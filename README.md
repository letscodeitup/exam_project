# exam_project

Simple project for DevOps practical:
- Node app (app.js) that returns "Hello from Docker"
- Dockerfile using Ubuntu base, installs Node.js and runs app.js
- To be deployed on EC2 with Docker
